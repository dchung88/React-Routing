import React from 'react';
import { Link } from '@reach/router';

const Number = props => {
    return (
        <div>
            <h1>Number: {props.num}</h1>
        </div>
    )
}

export default Number;